<?php $this->declareVars('content') ?>

<?php $this->content = $this->partial('partial/form', array(
    'form' => $this->discountForm,
    'controller' => $this->currentRoute()->getController(),
    'action' => $this->currentRoute()->getAction()
)); ?>

<?php $this->placeholder('contentFooter')->captureStart() ?>
    <button type="button" id="popup-shopping-cart-submit" onclick="payment.sendDiscountCouponForm()" class="btn btn-primary"><?php echo $this->translate('Submit') ?></button>
    <script type="text/javascript">
        $("#popup-shopping-cart-window").find("form:first").bind('submit', function() {
            payment.sendDiscountCouponForm();
            return false;
        });

        <?php if ($this->refreshPage) : ?>
            payment.hidePopupShopping();
            payment.refreshPage();
        <?php endif; ?>
    </script>
<?php $this->placeholder('contentFooter')->captureEnd() ?>

<?php 
    echo $this->partial('partial/popup', array(
        'popup_id' => 'popup-shopping-cart-window',
        'title' => 'Enter a discount code',
        'body' => $this->content,
        'footer' => $this->placeholder('contentFooter')
    )) 
?>